# 210. Course Schedule II
# Difficulty: Medium | Topic: Topological Sort
#
# There are numCourses courses. Given prerequisites, return the ordering
# of courses you should take to finish all courses. If impossible, return
# an empty array. If multiple valid answers, return any of them.
#
# Example: numCourses = 4, prerequisites = [[1,0],[2,0],[3,1],[3,2]] -> [0,2,1,3]
# Constraints: 1 <= numCourses <= 2000


class Solution:
    def findOrder(self, numCourses: int, prerequisites: list[list[int]]) -> list[int]:
        pass
