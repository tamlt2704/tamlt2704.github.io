# 703. Kth Largest Element in a Stream
# Difficulty: Easy | Topic: Heaps / Priority Queues
#
# Design a class to find the kth largest element in a stream.
# Note that it is the kth largest element in sorted order, not the kth
# distinct element.
#
# Example: KthLargest(3, [4,5,8,2]) -> add(3)=4, add(5)=5, add(10)=5
# Constraints: 1 <= k <= 10^4, 0 <= nums.length <= 10^4

import heapq


class KthLargest:
    def __init__(self, k: int, nums: list[int]):
        pass

    def add(self, val: int) -> int:
        pass
