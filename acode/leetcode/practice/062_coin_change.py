# 322. Coin Change
# Difficulty: Medium | Topic: 1-D DP
#
# Given an array of coin denominations and a total amount, return the
# fewest number of coins needed to make up that amount. Return -1 if
# that amount cannot be made up by any combination of the coins.
#
# Example: coins = [1,2,5], amount = 11 -> 3 (5+5+1)
# Constraints: 1 <= coins.length <= 12, 0 <= amount <= 10^4


class Solution:
    def coinChange(self, coins: list[int], amount: int) -> int:
        pass
