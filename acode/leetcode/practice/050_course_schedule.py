# 207. Course Schedule
# Difficulty: Medium | Topic: Topological Sort
#
# There are numCourses courses labeled 0 to numCourses-1. You are given
# an array prerequisites where prerequisites[i] = [ai, bi] means you
# must take course bi before course ai. Return true if you can finish
# all courses (i.e., no cycle in the prerequisite graph).
#
# Example: numCourses = 2, prerequisites = [[1,0]] -> true
# Constraints: 1 <= numCourses <= 2000, 0 <= prerequisites.length <= 5000


class Solution:
    def canFinish(self, numCourses: int, prerequisites: list[list[int]]) -> bool:
        pass
