# 146. LRU Cache
# Difficulty: Medium | Topic: Design
#
# Design a data structure that follows the constraints of a Least
# Recently Used (LRU) cache. Implement get and put in O(1) time.
# When the cache reaches capacity, evict the least recently used key.
#
# Example: LRUCache(2) -> put(1,1) -> put(2,2) -> get(1)=1 -> put(3,3) -> get(2)=-1
# Constraints: 1 <= capacity <= 3000, 0 <= key,value <= 10^4


class LRUCache:
    def __init__(self, capacity: int):
        pass

    def get(self, key: int) -> int:
        pass

    def put(self, key: int, value: int) -> None:
        pass
