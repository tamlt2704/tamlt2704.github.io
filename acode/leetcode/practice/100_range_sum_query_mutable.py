# 307. Range Sum Query - Mutable
# Difficulty: Medium | Topic: Segment Trees / BIT
#
# Given an integer array nums, handle multiple queries of two types:
# 1. Update the value of an element in nums.
# 2. Calculate the sum of elements between indices left and right inclusive.
#
# Example: NumArray([1,3,5]) -> sumRange(0,2)=9 -> update(1,2) -> sumRange(0,2)=8
# Constraints: 1 <= nums.length <= 3 * 10^4, at most 3 * 10^4 calls


class NumArray:
    def __init__(self, nums: list[int]):
        pass

    def update(self, index: int, val: int) -> None:
        pass

    def sumRange(self, left: int, right: int) -> int:
        pass
