# 208. Implement Trie (Prefix Tree)
# Difficulty: Medium | Topic: Tries
#
# Implement a trie with insert, search, and startsWith methods.
# - insert(word): Inserts the string word into the trie.
# - search(word): Returns true if word is in the trie.
# - startsWith(prefix): Returns true if any word starts with prefix.
#
# Example: Trie() -> insert("apple") -> search("apple")=true -> startsWith("app")=true
# Constraints: 1 <= word.length, prefix.length <= 2000, lowercase English


class Trie:
    def __init__(self):
        pass

    def insert(self, word: str) -> None:
        pass

    def search(self, word: str) -> bool:
        pass

    def startsWith(self, prefix: str) -> bool:
        pass
