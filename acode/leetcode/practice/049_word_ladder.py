# 127. Word Ladder
# Difficulty: Hard | Topic: Graph Traversal (BFS/DFS)
#
# Given beginWord, endWord, and a wordList, return the number of words
# in the shortest transformation sequence from beginWord to endWord,
# where each transformed word must exist in wordList. Only one letter
# can be changed at a time. Return 0 if no such sequence exists.
#
# Example: beginWord = "hit", endWord = "cog",
#          wordList = ["hot","dot","dog","lot","log","cog"] -> 5
# Constraints: 1 <= wordList.length <= 5000, words are same length


class Solution:
    def ladderLength(self, beginWord: str, endWord: str, wordList: list[str]) -> int:
        pass
