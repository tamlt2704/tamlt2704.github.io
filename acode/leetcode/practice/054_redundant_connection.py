# 684. Redundant Connection
# Difficulty: Medium | Topic: Union-Find
#
# Given a graph that started as a tree with n nodes (1 to n) with one
# additional edge added. Return the edge that can be removed so the
# result is a tree. If multiple answers, return the last one in input.
#
# Example: edges = [[1,2],[1,3],[2,3]] -> [2,3]
# Constraints: n == edges.length, 3 <= n <= 1000


class Solution:
    def findRedundantConnection(self, edges: list[list[int]]) -> list[int]:
        pass
