# 621. Task Scheduler
# Difficulty: Medium | Topic: Heaps / Priority Queues
#
# Given a char array tasks representing CPU tasks and an integer n
# (cooldown between same tasks), return the minimum number of intervals
# the CPU will take to finish all tasks.
#
# Example: tasks = ["A","A","A","B","B","B"], n = 2 -> 8
# Constraints: 1 <= tasks.length <= 10^4, 0 <= n <= 100


class Solution:
    def leastInterval(self, tasks: list[str], n: int) -> int:
        pass
