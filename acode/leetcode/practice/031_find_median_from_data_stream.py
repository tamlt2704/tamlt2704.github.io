# 295. Find Median from Data Stream
# Difficulty: Hard | Topic: Heaps / Priority Queues
#
# The median is the middle value in an ordered integer list. If the size
# is even, the median is the average of the two middle values.
# Implement MedianFinder that supports addNum and findMedian.
#
# Example: addNum(1), addNum(2), findMedian() -> 1.5, addNum(3), findMedian() -> 2.0
# Constraints: -10^5 <= num <= 10^5, at most 5 * 10^4 calls

import heapq


class MedianFinder:
    def __init__(self):
        pass

    def addNum(self, num: int) -> None:
        pass

    def findMedian(self) -> float:
        pass
