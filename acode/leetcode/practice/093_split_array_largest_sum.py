# 410. Split Array Largest Sum
# Difficulty: Hard | Topic: Binary Search
#
# Given an integer array nums and an integer k, split nums into k
# non-empty subarrays such that the largest sum of any subarray is
# minimized. Return the minimized largest sum.
#
# Example: nums = [7,2,5,10,8], k = 2 -> 18 ([7,2,5] and [10,8])
# Constraints: 1 <= nums.length <= 1000, 1 <= k <= min(50, nums.length)


class Solution:
    def splitArray(self, nums: list[int], k: int) -> int:
        pass
