# 994. Rotting Oranges
# Difficulty: Medium | Topic: Graph Traversal (BFS/DFS)
#
# In a grid, 0=empty, 1=fresh orange, 2=rotten orange. Every minute,
# any fresh orange adjacent (4-directionally) to a rotten one becomes
# rotten. Return the minimum minutes until no fresh orange remains,
# or -1 if impossible.
#
# Example: grid = [[2,1,1],[1,1,0],[0,1,1]] -> 4
# Constraints: m,n <= 10


class Solution:
    def orangesRotting(self, grid: list[list[int]]) -> int:
        pass
