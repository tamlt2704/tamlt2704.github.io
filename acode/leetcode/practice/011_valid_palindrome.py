# 125. Valid Palindrome
# Difficulty: Easy | Topic: Two Pointers
#
# A phrase is a palindrome if, after converting all uppercase letters to
# lowercase and removing all non-alphanumeric characters, it reads the
# same forward and backward.
#
# Example: s = "A man, a plan, a canal: Panama" -> true
# Constraints: 1 <= s.length <= 2 * 10^5, printable ASCII only


class Solution:
    def isPalindrome(self, s: str) -> bool:
        pass
