# 853. Car Fleet
# Difficulty: Medium | Topic: Stacks & Queues
#
# There are n cars going to the same destination along a one-lane road.
# Given position and speed arrays, return the number of car fleets that
# will arrive at the destination. A car fleet is cars that travel at the
# same speed because a faster car catches a slower one.
#
# Example: target = 12, position = [10,8,0,5,3], speed = [2,4,1,1,3] -> 3
# Constraints: n == position.length == speed.length, 1 <= n <= 10^5


class Solution:
    def carFleet(self, target: int, position: list[int], speed: list[int]) -> int:
        pass
