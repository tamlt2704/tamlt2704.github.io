# 875. Koko Eating Bananas
# Difficulty: Medium | Topic: Binary Search
#
# Koko has n piles of bananas. She can eat at speed k bananas/hour (one
# pile per hour, even if she finishes early). Given h hours, return the
# minimum integer k such that she can eat all bananas within h hours.
#
# Example: piles = [3,6,7,11], h = 8 -> 4
# Constraints: 1 <= piles.length <= 10^4, 1 <= h <= 10^9


class Solution:
    def minEatingSpeed(self, piles: list[int], h: int) -> int:
        pass
