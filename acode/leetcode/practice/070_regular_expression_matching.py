# 10. Regular Expression Matching
# Difficulty: Hard | Topic: 2-D DP
#
# Given a string s and a pattern p, implement regular expression matching
# with support for '.' (matches any single character) and '*' (matches
# zero or more of the preceding element).
#
# Example: s = "aa", p = "a*" -> true
# Constraints: 1 <= s.length <= 20, 1 <= p.length <= 20


class Solution:
    def isMatch(self, s: str, p: str) -> bool:
        pass
