# 1494. Parallel Courses II
# Difficulty: Hard | Topic: Advanced DP (Bitmask)
#
# Given n courses with prerequisites and you can take at most k courses
# per semester, return the minimum number of semesters to take all courses.
# Return -1 if impossible.
#
# Example: n = 4, relations = [[2,1],[3,1],[1,4]], k = 2 -> 3
# Constraints: 1 <= n <= 15, 1 <= k <= n


class Solution:
    def minNumberOfSemesters(self, n: int, relations: list[list[int]], k: int) -> int:
        pass
