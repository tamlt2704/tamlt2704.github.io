# 516. Longest Palindromic Subsequence
# Difficulty: Medium | Topic: DP on Sequences & Intervals
#
# Given a string s, find the longest palindromic subsequence's length.
# A subsequence is derived by deleting some or no elements without
# changing the order of the remaining elements.
#
# Example: s = "bbbab" -> 4 ("bbbb")
# Constraints: 1 <= s.length <= 1000, lowercase English letters


class Solution:
    def longestPalindromeSubseq(self, s: str) -> int:
        pass
