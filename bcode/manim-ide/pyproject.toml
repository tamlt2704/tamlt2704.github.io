[project]
name = "manim-ide"
version = "0.1.0"
description = "VSCode-like IDE component for Manim programming tutorials"
requires-python = ">=3.12"
dependencies = [
    "manim>=0.18.0",
    "manim-voiceover[gtts]>=0.3.0",
]

[tool.uv]
python = "3.12"
dev-dependencies = [
    "pytest>=8.0",
]
